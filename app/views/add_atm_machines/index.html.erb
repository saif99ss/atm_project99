<p id="notice"><%= notice %></p>

<h1>Listing Add Atm Machines</h1>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th>Name</th>
      <th>Street</th>
      <th>City</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @add_atm_machines.each do |add_atm_machine| %>
      <tr>
        <td><%= add_atm_machine.address %></td>
        <td><%= add_atm_machine.latitude %></td>
        <td><%= add_atm_machine.longitude %></td>
        <td><%= add_atm_machine.name %></td>
        <td><%= add_atm_machine.street %></td>
        <td><%= add_atm_machine.city %></td>
        <td><%= link_to 'Show', add_atm_machine %></td>
        <td><%= link_to 'Edit', edit_add_atm_machine_path(add_atm_machine) %></td>
        <td><%= link_to 'Destroy', add_atm_machine, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Add atm machine', new_add_atm_machine_path %>
